name: Continuous Integration & Deployment (GCP MLOps)

on:
  push:
    branches:
      - main
      - despliegue-modelos

env:
  PROJECT_ID: "tfm-ms-3"
  REGION: "europe-west1"
  WIF_PROVIDER: "projects/1076362823794/locations/global/workloadIdentityPools/github/providers/github-provider"
  WIF_SERVICE_ACCOUNT: "sa-github-deployer@tfm-ms-3.iam.gserviceaccount.com"

jobs:
  deploy-mlops:
    runs-on: ubuntu-latest
    permissions:
      contents: 'read'
      id-token: 'write'

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    # ==========================================
    # FASE AUTENTICACIÓN
    # ==========================================
    - name: Google Auth
      id: auth
      uses: google-github-actions/auth@v2
      with:
        workload_identity_provider: ${{ env.WIF_PROVIDER }}
        service_account: ${{ env.WIF_SERVICE_ACCOUNT }}
        token_format: 'access_token'

    - name: Login to Artifact Registry
      uses: docker/login-action@v3
      with:
        registry: ${{ env.REGION }}-docker.pkg.dev
        username: oauth2accesstoken
        password: ${{ steps.auth.outputs.access_token }}

    - name: Set up Cloud SDK
      uses: google-github-actions/setup-gcloud@v2

    # ==========================================
    # FASE A: COMPILAR Y SUBIR IMÁGENES DOCKER
    # ==========================================
    - name: Build and Push Preprocess Image
      run: |
        docker build -t europe-west1-docker.pkg.dev/tfm-ms-3/preprocess-repo/preprocess-pipeline:latest ./preprocess
        docker push europe-west1-docker.pkg.dev/tfm-ms-3/preprocess-repo/preprocess-pipeline:latest

    - name: Build and Push Dash Image
      run: |
        docker build -t europe-west1-docker.pkg.dev/tfm-ms-3/dash-repo/dash-pipeline:latest ./dash
        docker push europe-west1-docker.pkg.dev/tfm-ms-3/dash-repo/dash-pipeline:latest

    - name: Build and Push Monitoring Image
      run: |
        docker build -t europe-west1-docker.pkg.dev/tfm-ms-3/monitoring-repo/monitoring-pipeline:latest ./monitoring
        docker push europe-west1-docker.pkg.dev/tfm-ms-3/monitoring-repo/monitoring-pipeline:latest

    # ==========================================
    # FASE B: ACTUALIZAR Y DESPLEGAR EN CLOUD RUN
    # ==========================================
    - name: Update Preprocess Job
      run: |
        gcloud run jobs update preprocess \
          --image=europe-west1-docker.pkg.dev/tfm-ms-3/preprocess-repo/preprocess-pipeline:latest \
          --region=europe-west1

    - name: Deploy Cloud Run Service (Dash)
      run: |
        gcloud run services update dash \
          --image=europe-west1-docker.pkg.dev/tfm-ms-3/dash-repo/dash-pipeline:latest \
          --region=europe-west1

    - name: Deploy Cloud Run Service (Monitoring)
      run: |
        gcloud run services update monitoring \
          --image=europe-west1-docker.pkg.dev/tfm-ms-3/monitoring-repo/monitoring-pipeline:latest \
          --region=europe-west1

    # ==========================================
    # FASE C: DISPARAR EJECUCIÓN AUTOMÁTICA
    # ==========================================
    - name: Execute Preprocess Job Automatically
      run: |
        echo "[*] Disparando la ejecución del Job de preprocesamiento en Cloud Run..."
        gcloud run jobs execute preprocess \
          --region=${{ env.REGION }} \
          --async